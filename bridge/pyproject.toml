[project]
name = "bridge"
version = "0.1.0"
description = "Standalone BLE development bridge for the Talisman prototype"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bleak>=3.0.2",
]
